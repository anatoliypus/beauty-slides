import { choosedObjectType, SlideType } from '../model/model';

export default function slidesReducer(state: Array<SlideType> = [], action: any, currSlideId: string | null, choosedObject: choosedObjectType): Array<SlideType> {
    return state
}